$(document).ready(function() {
  $('button').button();
  $('ourbutton').button();

  $('#nextquestion').click(function() {
    // Relies on the ladder hamlet being imported
    window.location.href = $('#nextURL')[0].href;
  });
});